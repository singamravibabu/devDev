# list min() and max() functions
# the min(list) function returns the smallest item in a list
# the max(list) function returns the largest item in a list
values = [10, 85, 99, 74, 65, 25, 45, 35, 15, 5]
print('Minimum:', min(values))
print('Maximum:', max(values))